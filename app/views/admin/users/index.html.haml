%h1 Admin Dashboard
- if flash[:alert]
    .alert=flash[:alert]
.row
    .card-col6
        .card-header
            = render 'show_all_users'


    .card-col6
        = link_to 'Invite new user', new_user_invitation_path