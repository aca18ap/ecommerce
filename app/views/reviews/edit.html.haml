%h1 Editing review

= render 'editform'

- if @error_msg.to_i == 1
  %p1= "Rank shoudld be 0 if review is hidden from the front page"

- if @error_msg.to_i == 2
  %p1= "Rank should not be 0 for review to be shown on the front page"

%br

= link_to 'Show', @review
\|
= link_to 'Back', reviews_path
