.nested-fields.product-material-fields
    .form-inline
        .material_from_list.d-flex
            .flex-fill
                .d-inline-block#material= f.association :material, collection: Material.order(:name)
            .flex-fill
                .d-inline-block
                    .d-block
                        .row= f.label :percentage
                        .l-50.row= f.text_field :percentage, { :class => 'percentage'}

        = link_to_remove_association f, class: 'btn btn-default btn-xs' do
            Remove Material

