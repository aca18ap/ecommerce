%ul
    - categories.each do |category, children|
        %li.nav-item
            .d-flex
                .nav-link.flex-grow-0=link_to_unless_current "#{category.name}", category
            
                - if children.present?
                    %button.btn{type: 'button', 'data-bs-toggle'=>"collapse", 'data-bs-target'=>"#collapse#{category.id}", 'aria-expanded'=>'false', 'aria-controls'=>"collapse#{category.id}"} 
                        .fa.fa-chevron-down.small
            .collapse{id: "collapse#{category.id}"}
                =render 'submenu_categories', categories: children if children.present?